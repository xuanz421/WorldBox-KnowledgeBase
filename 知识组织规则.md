---
title: 知识组织规则
aliases:
  - Knowledge Organization Rules
  - Obsidian Rules
---

# 知识组织规则（Obsidian）

本文件是 WBKB 知识库（Obsidian vault）结构组织的 authoritative 规则，自 AGENTS.md 独立。适用于 `knowledge/` 三个总索引内部与所有知识文档的整理；根导航入口为 [知识库索引](知识库索引.md)。

知识库是索引型森林，不是高密度知识图网络。

## Top-Level Index Authority

总索引固定三个，属于用户控制的顶层信息架构：

* 源代码索引 — `knowledge/systems/`：WorldBox 原版源代码解析
* 模组索引 — `knowledge/reference-mods/`：参考模组自身实现解析
* 模块索引 — `knowledge/patterns/`：可复用实现（导航文件 `模式索引.md`，节点显示名「模块索引」）

未经用户明确要求，Agent 不得：新增 / 删除 / 合并总索引、修改总索引名称、调整总索引之间层级、将内部方向提升为新总索引、为图谱整齐强行制造目录层级、为消除孤立节点创建虚假关系。发现内部方向大到可能值得成为新总索引时，只记录并汇报，不直接修改。

## Index Responsibilities

* 源代码索引：原版类 / 方法 / 字段 / 属性 / 调用关系 / 数据流 / 系统结构 / 状态变化 / 存档 / UI / Asset / Actor / Kingdom / City / World 等解析结果
* 模组索引：模组总体架构 / 功能体系 / 目录结构 / 关键系统 / 代码入口 / 状态管理 / 数据流 / Patch 逻辑 / 存档机制 / 与原版交互 / Bug / 风险 / 具体机制深入解析。不建与模组解析职责重叠的「研究索引」；模组专项研究归入该模组节点内部（如 biology 研究子文档挂 `mods/biology/`）
* 模块索引：从源码与模组中整理、验证、抽象出的可复用实现（代码 / 接入模板 / Patch / 注册 / UI / 状态管理 / 存档 / 事件 / 工具类 / 数据结构 / 算法 / 已验证方案 / 组合实现）。判断标准：「以后开发 Mod 时能否直接拿来参考、调用、组合或改造？」一级分类按应用方向自然形成，不预建固定分类；内容较少的方向集中在方向主 md，机制边界清晰且被独立调用的大模块单独成文

模组索引与模块索引职责分离：前者回答「它原本怎么实现」，后者回答「我们整理出什么可复用」。同一证据两处可用；不因内容进入模块索引而删除模组索引中的原始解析。

## Naming Rules

* 代码实体保持原命名（`Actor` / `ActorTrait` / `ActorWoundState` / `BuildOrgans`），不翻译不改写
* 模组主节点使用模组正式名称，优先级：正式中文名 > 正式英文名 > 仓库名 / 项目名；不自行翻译第三方模组名
* 知识库自建的组织 / 机制节点可用中文可读名（如「器官与健康」「Actor 状态管理」）
* 技术性词语保持英文原文（class / method / field / API / Harmony prefix / postfix / Transpiler / frontmatter / stable ID / catalog 等），不在标题或术语位置翻译或改写；需要解释时，在正文内容中另行以中文说明该词含义

## File Name vs Display Name

* 不为图谱显示效果修改文件名或路径——文件名承担稳定路径、Git 引用、Agent 定位、Markdown 链接目标
* 人类可读显示名通过 frontmatter `title` 提供；`aliases` 仅在存在实际搜索价值（常用别名 / 英文名 / 旧名 / 缩写）时添加，不强制统一格式

```yaml
---
title: 器官与健康
aliases:
  - Organs Health
---
```

## Recursive Forest

Forest 不限制固定层数，允许递归分枝：

- Index / Branch / Leaf 是结构角色，不是固定文件类型。
- Leaf 只是"当前没有子节点"的结构角色，不是永久终点；Leaf 膨胀后可提升为 Branch，并继续拆出子 md。
- 拆分依据优先级：1. mechanism boundary → 2. agent consumption/read cost → 3. document size；不按固定行数机械拆分。
- 不按 class / method / symbol 一项一个 md；一个子 md 应回答一个相对完整的机制问题。

**Leaf 升级为 Branch 的判定**（指导性，非硬性；出现以下任意多项时考虑拆分）：

- 一个文件包含两个或以上可独立理解的机制闭环
- 多个独立生命周期已经各自形成较长章节
- 单个章节本身已经可以独立成为文档
- Agent 为回答局部问题经常需要读取大量无关内容
- 文档持续增长且后续已有明确的新机制要加入
- definition/content table 与 mechanism explanation 大量混杂

行数只作为信号：约 300–500 行是 review trigger，不能作为强制拆分阈值。

**Branch / Index 文档职责**：只保留 Scope、核心对象/机制总览、子主题导航、推荐阅读顺序、当前 Evidence / Verification Status、Unknown / Deferred 总览；不复制子文档详细机制（父文档摘要 ≠ 子文档内容重复）。

典型结构：

```text
Root Index
└─ System / Mod
   ├─ Mechanism A
   └─ Mechanism B
      ├─ B1
      └─ B2
```

## Weak Balance

森林不要求严格平衡（AVL 思想，不追求数学均衡），但应避免单个 Branch 持续吸收异质子方向、退化为万能容器。方向过宽时可横向拆分、子方向提升、同类重组、建立新同级方向——仅在总索引内部，且判断优先级同 Recursive Forest（mechanism boundary → read cost → size）；不为分支数量接近强行拆分。

Review signals（非硬性阈值）：直接子方向约 6–8 个；子方向形成多个机制簇；某子方向规模接近兄弟总和；阅读某节点需先理解大量无关兄弟节点；新内容越来越难归类；出现泛化容器（`Misc` / `Other` / 过度宽泛的 `System`）。

## Link Rules

链接必须具有真实意义。允许：父子归属、索引导航、强依赖、必要引用、稳定证据来源。方向只沿 index / branch → child；外部索引优先链接 Branch，Branch 负责管理自己的 child docs；跨 system / tree / sibling 一律使用 stable ID（`pattern_id` / `ref:<mod-id>` / system 文件路径），不建叶子横向互链。

禁止：无意义横向互链；为 Graph View 效果增加双向链接；为消除孤立节点补回链或虚假关系；为图谱视觉完整强行加链接。

**连接 ≠ 相关**（永久性判定纪律）：

- 「有连接需要」指**导航需要**，不是「内容有相关性」。内容相关性由 stable ID / 纯文本 provenance 标注承载（机器与人都可解析），不需要 markdown link。
- 判定一条链接是否该存在，用**扩展测试**：如果这类链接在同类场景下会随条目数量线性增长（如「每个研究完成的 mod 都从批次台账连一条线到 profile」），它就是重复索引，不是导航——同类引用一律使用 stable ID。
- 反例教训（R2 台账 → biology profile，已纠正）：批次追踪文档（batch tracker）引用已完成 mod 时只写 `ref:<mod-id>`，不加 markdown link；台账类文档唯一的入链来自根索引（一条，不随条目增长），周期结束后随归档移除、转为合法孤立历史记录。
- provenance（来源/批次标注）是单向文字注记，永不反向建链。

## Independent Markdown Files

孤立 md 是合法状态：孤立节点 ≠ 结构错误，不要求全库形成单一连通图。但孤立资格必须**同时**满足两个条件：

1. **无连接性** —— 当前与任何现有节点不存在真实关系（归属、依赖、导航、证据来源）
2. **无连接需求** —— 内容没有归属某个 Branch 的必要、没有稳定导航诉求、没有需要暴露给消费者的路径

由此得到双向强制规则：

- 有连接需要（明确归属某 Branch / 需要稳定导航 / 与某节点存在真实依赖或证据关系）→ **必须接入森林**，不允许游离在外
- 无连接需要 → **尽可能不连接**，保持独立；不为了图谱视觉完整强行接入

两个方向都禁止劣化：不允许「实际承担导航或归属职责却游离在森林之外」的中间状态；也不允许把本应独立的文档强行接入主树。

例外：`knowledge/patterns/` 与 `knowledge/reference-mods/` 受 validators 强制结构约束（catalog 四处同步、散落 `.md` 拒收，见 AGENTS.md Editing knowledge/），不适用孤立规则，条目必须入 catalog。

## Placement Decision

```text
WorldBox 原版源代码的解析结果        → 源代码索引
参考模组自身的架构/代码/机制解析     → 模组索引
可供后续 Mod 开发快速复用的实现      → 模块索引
无连接性且无连接需求                 → 允许独立 md（尽可能不连接）
```

不因内容来源于某个模组就阻止其整理后进入模块索引；不因实现有复用价值就删除模组索引中的原始解析记录。

## Maintenance Checklist

整理知识库时：先读取现有结构与相关文档；尊重已确定的目录 / 命名 / 索引职责；不因局部优化推翻已有方案；优先小范围、可验证修改；不为「统一」做无差别批量改写。

修改后至少检查：链接有效；内容无丢失；stable ID 保持；无重复索引；无无意义横向链接；代码实体名与模组正式名未被误改；树结构未破坏；未将本应独立的文档强行接入主树；未将有连接需要的文档遗留为孤立状态。涉及大量结构迁移时，先输出方案与影响范围再实施。
